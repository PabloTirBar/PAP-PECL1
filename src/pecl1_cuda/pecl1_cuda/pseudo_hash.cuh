#ifndef PSEUDO_HASH_CUH
#define PSEUDO_HASH_CUH

typedef unsigned char byte;

// Función pública para usar desde el main
void mostrarPseudoHash(byte* h_pixels, int width, int height, int bpp);

#endif